when I upload photo in activity section id doesn't upload the photo and dosen't show the photo in the activity section. make sure the photo is uploaded properly and it is shown in the activity section. also make sure the photo is properly styled and it looks good on all screen sizes.

/dashboard/page_students.php?id=page_students in this page the cards are shrinking. make the cards responsive (flex wrap) and make sure they look good on all screen sizes. The popup modal which appears at pressing add student button is not properly shown. the modals is not responsive and it is not properly centered on the screen. make sure the modal is responsive and it is properly centered on the screen. also make sure the modal is properly styled and it looks good on all screen sizes. The modal can be close easily.

when I delete any photo just the database record is deleted but the photo is not deleted from the server. make sure when I delete any photo it is deleted from the server as well. also make sure when I delete any photo it is deleted from the activity section as well.

in /dashboard/page_photos.php?id=page_photos page when pressed the insert photo button the popup modal is not properly shown. the modals is not responsive and it is not properly centered on the screen. make sure the modal is responsive and it is properly centered on the screen. also make sure the modal is properly styled and it looks good on all screen sizes. The modal can be close easily.

use dry method as much as possible specially popup and image uploading section


imporve the ux of the admin dashboard. make sure the admin dashboard is easy to use and it is easy to navigate. also make sure the admin dashboard is properly styled and it looks good on all screen sizes. use proper toast button.

when I tries to add any journal from the dashboard it shows in there but not in the main website page like conference tab

add a file that a github action that will automatically deploy the website to the server when I push any changes to the main branch. make sure the github action is properly configured and it works properly. also make sure the github action is properly documented and it is easy to understand.

/dashboard/page_slider.php in this page the image doesn't upload properly. make sure the image is uploaded properly and it is shown in the slider section.

after insert any image in the admin dashboard and when it finishes why the modal still block the scren?

make a button to go to the admin dashboard